# E-mail_Verifier

This is a basic email verifier tool. 
In this tool, it checks the MX records, SPF records and DMARC records of DNS of the given domain.
If all these 3 records are true, then the given domain is valid, otherwise it is invalid.
